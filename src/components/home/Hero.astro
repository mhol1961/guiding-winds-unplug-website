---
// Full-bleed cinematic hero. Crossfades 4 real photos from Dodie's library
// (sunset catamaran, anchorage, foredeck, cockpit sunrise). Honors
// prefers-reduced-motion. Headline overlay sits low-left, magazine-style.

interface Scene {
  src: string;
  alt: string;
}

interface Props {
  scenes?: Scene[];
  eyebrow: string;
  headlinePrefix: string;
  headlineEmphasis: string;
  headlineSuffix?: string;
  subline: string;
  primaryCta: { label: string; href: string };
  secondaryCta: { label: string; href: string };
  trustItems: string[];
}

const {
  scenes = [
    { src: '/img/source/hero/02-sunset-catamaran-anchorage.jpg', alt: 'A catamaran at anchor at sunset in the Caribbean, warm orange sky reflecting off glassy water' },
    { src: '/img/source/hero/01-catamaran-green-headland.jpg', alt: 'A catamaran sailing past a green tropical headland in the British Virgin Islands' },
    { src: '/img/source/hero/03-foredeck-bow-sun-flare.jpg', alt: 'Looking out from the foredeck of a catamaran toward the horizon, sun flare across the bow' },
    { src: '/img/source/hero/04-cockpit-sunrise.jpg', alt: 'Coffee on the cockpit bench at sunrise on a sailing catamaran' },
  ],
  eyebrow,
  headlinePrefix,
  headlineEmphasis,
  headlineSuffix = '',
  subline,
  primaryCta,
  secondaryCta,
  trustItems,
} = Astro.props;

const sceneDuration = 7;
const totalDuration = sceneDuration * scenes.length;
---

<section class="hero relative w-full min-h-[100dvh] overflow-hidden bg-primary text-bone">
  <div class="absolute inset-0 z-0" aria-hidden="true">
    {
      scenes.map((scene, i) => (
        <div
          class="hero-scene absolute inset-0 opacity-0"
          style={`--i: ${i}; --total: ${scenes.length}; --dur: ${totalDuration}s;`}
        >
          <img
            src={scene.src}
            alt=""
            loading={i === 0 ? 'eager' : 'lazy'}
            decoding="async"
            fetchpriority={i === 0 ? 'high' : 'auto'}
            class="absolute inset-0 w-full h-full object-cover"
          />
        </div>
      ))
    }
    {/* Editorial overlay — darker at the bottom for legibility, lighter top */}
    <div class="absolute inset-0 bg-gradient-to-b from-primary/40 via-primary/10 via-50% to-primary/90"></div>
  </div>

  {/* Foreground content */}
  <div class="relative z-10 min-h-[100dvh] flex flex-col">
    <div class="flex-1 flex items-end pb-24 lg:pb-32 px-7 lg:px-14 pt-32">
      <div class="max-w-[1400px] w-full mx-auto">
        <div class="max-w-4xl">
          <p class="font-label-caps uppercase tracking-[0.28em] text-[0.75rem] font-semibold text-bone/90 mb-8">
            {eyebrow}
          </p>
          <h1
            class="font-display text-bone font-light text-pretty leading-[0.95] tracking-[-0.03em]"
            style="font-size: clamp(2.75rem, 7.5vw + 1rem, 7rem); font-variation-settings: 'opsz' 144, 'SOFT' 30, 'WONK' 0;"
          >
            {headlinePrefix}
            <span
              class="italic block sm:inline"
              style="font-variation-settings: 'opsz' 144, 'SOFT' 60, 'WONK' 1;"
            >
              {headlineEmphasis}
            </span>
            {headlineSuffix}
          </h1>
          <p class="font-body-lg text-[1.0625rem] lg:text-[1.1875rem] leading-[1.6] text-bone/90 mt-8 max-w-[58ch]">
            {subline}
          </p>
          <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 mt-10 lg:mt-12">
            <a
              href={primaryCta.href}
              class="group inline-flex items-center gap-3 cursor-pointer text-bone font-label-caps uppercase tracking-[0.22em] text-[0.75rem] font-semibold pb-1 border-b-2 border-accent hover:text-accent transition-colors"
            >
              {primaryCta.label}
              <svg viewBox="0 0 16 16" class="size-3 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <path d="M1 8h13M9 3l5 5-5 5" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
            </a>
            <a
              href={secondaryCta.href}
              class="group inline-flex items-center gap-3 cursor-pointer text-bone/80 font-label-caps uppercase tracking-[0.22em] text-[0.75rem] font-semibold pb-1 border-b border-bone/30 hover:text-bone hover:border-bone transition-all"
            >
              {secondaryCta.label}
              <svg viewBox="0 0 16 16" class="size-3 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                <path d="M1 8h13M9 3l5 5-5 5" stroke-linecap="round" stroke-linejoin="round" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </div>

    {/* Trust strip */}
    <div class="border-t border-bone/15 backdrop-blur-md bg-primary/30">
      <div class="max-w-[1400px] mx-auto px-7 lg:px-14 py-5 flex flex-wrap items-center gap-x-10 gap-y-3">
        {
          trustItems.map((item) => (
            <span class="inline-flex items-center gap-2.5 font-label-caps uppercase tracking-[0.24em] text-[0.625rem] font-semibold text-bone/85">
              <span class="block size-[6px] bg-accent rounded-full" aria-hidden="true" />
              {item}
            </span>
          ))
        }
      </div>
    </div>
  </div>
</section>

<style>
  /* Crossfade keyframes — each scene visible ~25% of the cycle */
  .hero-scene {
    animation: hero-fade var(--dur) infinite;
    animation-delay: calc(var(--i) * (var(--dur) / var(--total) * -1));
    will-change: opacity;
  }
  @keyframes hero-fade {
    0% { opacity: 0; }
    5% { opacity: 1; }
    25% { opacity: 1; }
    30% { opacity: 0; }
    100% { opacity: 0; }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-scene { animation: none; }
    .hero-scene:first-child { opacity: 1; }
  }
  /* Hero img Ken Burns — independent of crossfade so motion stays even */
  .hero-scene img {
    animation: hero-zoom calc(var(--dur) / var(--total) * 2) ease-out infinite alternate;
  }
  @keyframes hero-zoom {
    from { transform: scale(1); }
    to { transform: scale(1.08); }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero-scene img { animation: none; }
  }
</style>
